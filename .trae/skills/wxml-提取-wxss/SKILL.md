---
name: WXML 提取 WXSS
description: 读取当前 WXML 中的 class，自动生成 WXSS 样式骨架。
---

请分析选中的 WXML 代码，提取其中所有的 `class` 属性，并为我生成对应的 WXSS 基础骨架代码。
要求：
1. 保持 CSS 类的层级嵌套关系（如果可以推断出来）。
2. 为每个类名留下空的大括号 `{}`，并在大括号前加上类名。
3. 使用小程序的响应式单位 `rpx`。
4. 自动加入一些常见的 Flex 布局提示注释，方便我后续填写。
5. 不要修改原始的 WXML 代码，只输出 WXSS 代码。